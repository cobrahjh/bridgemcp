#!/usr/bin/env node

/**
 * BridgeMCP CLI
 *
 * Usage:
 *   npx bridgemcp          # Start server (HTTP + MCP if piped)
 *   npx bridgemcp --mcp    # Force MCP mode
 *   npx bridgemcp -v       # Verbose logging
 */

require('./index.js');
